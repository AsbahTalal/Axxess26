{
  "name": "axxess26-pediatric-pulse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.0",
    "autoprefixer": "^10.4.24",
    "firebase": "^12.9.0",
    "postcss": "^8.5.6"
  }
}
